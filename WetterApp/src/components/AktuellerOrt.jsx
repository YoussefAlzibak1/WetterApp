import React, { useContext } from 'react';

const AktuellerOrt = () => {

const {  } = useContext(contextWetter);

    return (
        <div>
            
        </div>
    );
}

export default AktuellerOrt;

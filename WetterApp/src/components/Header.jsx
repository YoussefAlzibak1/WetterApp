import React, { useContext } from 'react';

const Header = () => {

    const {  } = useContext(contextWetter);

    return (
        <div>
            
        </div>
    );
}

export default Header;

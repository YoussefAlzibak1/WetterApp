import React, { useContext } from 'react';

const StundenWetter = () => {

    const {  } = useContext(contextWetter);

    return (
        <div>
            
        </div>
    );
}

export default StundenWetter;
